{
  "name": "rrweb-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {},
  "scripts": {
    "rrweb-build": "pnpm -F @joyo-dev1/react-components build",
    "playground-dev": "pnpm -F @joyo-dev1/playground start",
    "playground-build": "pnpm -F @joyo-dev1/playground build"
  }
}